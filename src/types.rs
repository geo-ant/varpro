use nalgebra::base::{Scalar,Dim};
use nalgebra::Matrix;
use nalgebra::base::U1;
use nalgebra::base::storage::Owned;

// pub type ParameterVectorType<NumType : Scalar, Rows : Dim> = Matrix<NumType,Rows,U1, Owned<NumType,Rows,U1>>;
// pub type DataVectorType<NumType: Scalar, Rows: Dim>
// //pub type ModelfunctionType<NumType : Scalar, Rows : Dim, S> = Fn<>
